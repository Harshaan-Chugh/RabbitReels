events {
    worker_connections 1024;
}

http {
    proxy_buffer_size 128k;
    proxy_buffers 4 256k;
    proxy_busy_buffers_size 256k;
    proxy_max_temp_file_size 0;

    # Upstream API server
    upstream api_backend {
        server rabbitreels-api-1:8080;
    }

    # HTTP to HTTPS redirect (disabled for local dev)
    # server {
    #     listen 80;
    #     server_name rabbitreels.us www.rabbitreels.us;
    #     return 301 https://rabbitreels.us$request_uri;
    # }

    # HTTPS server (disabled for local dev)
    # server {
    #     listen 443 ssl http2;
    #     server_name rabbitreels.us www.rabbitreels.us;

    #     # SSL configuration
    #     ssl_certificate /etc/letsencrypt/live/rabbitreels.us/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/rabbitreels.us/privkey.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    #     ssl_prefer_server_ciphers off;
    #     ssl_session_cache shared:SSL:10m;
    #     ssl_session_timeout 10m;

    #     # Security headers
    #     add_header X-Frame-Options DENY;
    #     add_header X-Content-Type-Options nosniff;
    #     add_header X-XSS-Protection "1; mode=block";
    #     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    #     location /api/ {
    #         proxy_pass http://api_backend;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }

    #     location / {
    #         proxy_pass http://web:80;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}